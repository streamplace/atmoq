# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.85"
# Published under a fork name so `cargo install atmoq` works from crates.io
# while the two additive patches (see UPSTREAM.md) await upstream consensus.
# The lib name stays `moq_net`, so consumers use dependency renaming:
#   moq-net = { package = "atmoq-moq-net", version = "..." }
name = "atmoq-moq-net"
version = "0.1.10"
authors = ["Luke Curley", "Eli Mallon <eli@aquareum.tv>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "TEMPORARY fork of moq-net 0.1.10 with two additive replay-window patches, pending upstreaming to moq-dev/moq. Use moq-net unless you are atmoq."
readme = "README.md"
keywords = [
    "quic",
    "http3",
    "webtransport",
    "media",
    "live",
]
categories = [
    "multimedia",
    "network-programming",
    "web-programming",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/streamplace/atmoq"
resolver = "2"

[features]
serde = []

[lib]
name = "moq_net"
path = "src/lib.rs"

[dependencies.bytes]
version = "1"

[dependencies.futures]
version = "0.3"

[dependencies.kio]
version = "0.3"

[dependencies.num_enum]
version = "0.7"

[dependencies.rand]
version = "0.10.1"

[dependencies.serde]
version = "1"
features = [
    "derive",
    "rc",
]

[dependencies.serde_json]
version = "1"

[dependencies.thiserror]
version = "2"

[dependencies.tokio]
version = "1.48"
features = [
    "macros",
    "io-util",
    "sync",
    "test-util",
    "time",
]

[dependencies.tracing]
version = "0.1"

[dependencies.web-async]
version = "0.1.3"
features = ["tracing"]

[dependencies.web-transport-trait]
version = "0.3.4"
