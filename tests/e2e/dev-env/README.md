# dev-env (vendored)

Minimal in-process atproto network (PLC + PDS) for tests.

Provenance: copied from `streamplace/js/dev-env`, which was copied from Cardcore,
which was copied from
[atcute's internal dev-env](https://github.com/mary-ext/atcute/tree/trunk/packages/internal/dev-env)
(MIT licensed). Fourth copy and counting; if it grows real features, extract it
to a shared package.

Local changes vs the streamplace copy: none yet (only the package structure
around it differs — see `../package.json`).
